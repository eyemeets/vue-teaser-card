module.exports = {
  content: [
    './index.html', // Main HTML file
    './src/**/*.{vue,js,ts}', // All Vue, JS, and TS files in src folder
    './src/**/*.css', // All CSS files in src folder
    './public/**/*.html' // Any other HTML files in public (if necessary)
  ]
}